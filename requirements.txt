# 核心Web框架
streamlit>=1.28.0,<2.0.0

# 数据处理核心库
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# 可视化库
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Excel文件处理
openpyxl>=3.1.0,<4.0.0  # 读取/写入xlsx文件
xlrd>=2.0.0,<3.0.0      # 兼容旧版xls文件（如需）

# Markdown处理
markdown>=3.4.0,<4.0.0

# 可选：提升性能/兼容性
python-dotenv>=1.0.0    # 如需环境变量配置（可选）